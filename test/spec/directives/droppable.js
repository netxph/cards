(function() {
    'use strict';

    describe('Directive: droppable', function () {

        beforeEach(module('cardsApp'));

        var element,
        scope;

        beforeEach(inject(function ($rootScope) {
            scope = $rootScope.$new();
        }));

        it('should element is droppable', inject(function ($compile) {
        }));
    });
})();
